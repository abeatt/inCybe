{
  "name": "incybe-obsidian-plugin",
  "version": "1.0.0",
  "description": "inCybe Obsidian Integration",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild main.ts --bundle --watch --platform=browser --external:obsidian --format=cjs --outfile=main.js",
    "build": "esbuild main.ts --bundle --minify --platform=browser --external:obsidian --format=cjs --outfile=main.js"
  },
  "keywords": [],
  "author": "Giblets Creations",
  "devDependencies": {
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "typescript": "^5.0.0"
  }
}